div(class="jumbotron")
    h3(style="color: blue;") Chat WebSocket
    br
    form(action="/mensajes" method='post')
        div(class="form-group")
            label(for="user")
                b Usuario
            input(id="user" class="form-control" name="user" type="email" required)
        div(class="form-group")
            label(for="mensaje")
                b Mensaje
            textarea(id="contenidoMensaje" name="mensaje", cols="3" class="form-control") 
        button(class="btn btn-success mt-3 mb-5 btn-block" onclick="enviarMensaje()") Enviar
    hr
    div(id="historialChat")
    
